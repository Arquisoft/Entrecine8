@model IEnumerable<EntrecineWebApp.Models.Sesion>

@{
    ViewBag.Title = "EditarSesiones";
}

<h2>EditarSesiones</h2>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SalaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PeliculaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescuentoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SalaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PeliculaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescuentoId)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", "Sesion", new { id = item.Id }, new { }) |
            @Html.ActionLink("Borrar", "Delete", "Sesion", new { id = item.Id }, new { })
        </td>
    </tr>
}

</table>

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

